# Звіт з виконання Work-case №1
**Дисципліна:** Операційні системи  
**Студент:** Фокін Степан Володимирович  
**Група:** РПЗ-33  
**Команда:** №1

---

## 1. Призначення Git та основні команди

**Git** — це розподілена система керування версіями, яка використовується для:
* Відстеження історії змін у файлах проєкту.
* Спільної роботи кількох розробників над одним кодом.
* Можливості повернення до попередніх версій продукту.

### Основні команди Git:
* `git init` — створює новий локальний репозиторій.
* `git clone [url]` — копіює віддалений репозиторій на локальну машину.
* `git status` — показує стан робочого дерева (змінені, нові файли).
* `git add .` — додає всі змінені файли до індексу (staging area).
* `git commit -m "опис"` — фіксує зміни в історії з коментарем.
* `git push origin main` — відправляє локальні коміти на сервер (напр. GitHub).
* `git pull origin main` — завантажує останні зміни з сервера та об'єднує їх із локальними.
* `git log` — виводить список усіх комітів у репозиторії.

---

## 2. Що таке «коміт»?

**Коміт (Commit)** — це зафіксований стан файлів у певний момент часу. Кожен коміт дозволяє відстежувати зміни таким чином:
* **Збереження історії:** Git створює "злімок" усіх файлів, що дозволяє бачити, хто, коли і що саме змінив.
* **Порівняння:** Можна порівняти два коміти, щоб побачити додані чи видалені рядки коду.
* **Безпека:** Якщо нова функція зламала проєкт, можна миттєво відкотитися до попереднього успішного коміту.

---

## 3. Репозиторій проєкту

Мій публічний репозиторій для зберігання всіх навчальних робіт:
🔗 **[University-Projects (GitHub)](https://github.com/FocynStepan/University-Projects)**

---

## 4. Висновок
Під час виконання Work-case №1 я зареєстрував акаунт на GitHub, ініціалізував репозиторій та навчився базовим операціям: додаванню файлів, створенню комітів та синхронізації з віддаленим сервером.